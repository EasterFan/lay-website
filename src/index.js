// 导入 Chrome API 模拟
import './chrome-mock';

// 导入样式
import './styles.css';

// 这里可以添加你的应用初始化代码
document.addEventListener('DOMContentLoaded', () => {
  // 初始化应用
  console.log('Web version initialized');
}); 